# Second Me OAuth2 配置（必填）
SECONDME_CLIENT_ID=your_client_id
SECONDME_CLIENT_SECRET=your_client_secret
SECONDME_API_BASE_URL=https://app.mindos.com/gate/lab

# OAuth 回调地址（可选）
# 本地开发无需设置，默认使用 http://localhost:3000/api/auth/callback
# Vercel 部署时会自动从 VERCEL_PROJECT_PRODUCTION_URL 推断（稳定的生产域名），也可以手动指定:
# SECONDME_REDIRECT_URI=https://your-domain.vercel.app/api/auth/callback

# Kimi (Moonshot AI) 世界模型
# 从 https://platform.moonshot.cn 获取 API Key
# 不设置则使用本地引擎（无 AI 也可运行）
KIMI_API_KEY=

# 数据库（PostgreSQL）
# 推荐使用 Neon（免费）: https://neon.tech
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Next.js
NEXTAUTH_SECRET=your_random_secret_here
# Vercel 部署时设置为你的实际域名（也会用于推断 redirect_uri）
NEXTAUTH_URL=http://localhost:3000
